services:
  php:
    build: ./php-app
    ports:
      - "8080:80"
    env_file:
      - .env
    volumes:
      - ./php-app/src:/var/www/html
      - ./php-app/data:/var/www/html/data
    restart: unless-stopped
