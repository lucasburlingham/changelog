services:
  php:
    build: ./php-app
    ports:
      - "8080:80"
    volumes:
      - ./php-app/src:/var/www/html
      - ./php-app/data:/var/www/html/data
      - ./settings.ini:/var/www/html/settings.ini:ro
    restart: unless-stopped
